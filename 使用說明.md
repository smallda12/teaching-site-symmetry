# 線對稱圖形｜教學網 使用說明

數學領域　115 學年度第 1 學期　第 5 單元

---

## 怎麼打開

| 方式 | 版本 | 差別 |
|---|---|---|
| **雙擊 `index.html`** | 教室版（右上角黃標） | **可以上傳學生照片**做點名 |
| 用網址開啟 | 公開版（右上角藍標） | 照片上傳自動關閉 |

線上版：https://smallda12.github.io/teaching-site-symmetry/

學生的姓名與照片**只存在那台電腦的瀏覽器裡**，不會寫進檔案、也不會上傳到網路。
換電腦要帶名單，用點名區的「匯出／匯入 JSON」。

---

## 七個分區

| 分區 | 上課可以怎麼用 |
|---|---|
| 🙋 **點點名** | 照片＋姓名抽籤。一輪每人只點一次，全班點完會自動停 |
| 🃏 **圖詞輪播** | 22 張學習圖卡，詞和說明各是一顆發音鈕，可以重複點 |
| 🎬 **教學影片** | 完整版；**右邊的清單可以直接跳到某一個重點的短片** |
| 📚 **教材教學** | 七個重點全部開放、不必照順序。每個重點：教學頁 → 3 題評量 |
| 📝 **評量區** | 21 題總評量＋各重點正確率。**答錯不重答**，看一眼正解就進下一題 |
| 🧠 **自學區** | 圖卡點一下放大，可自動輪播；適合掛著當背景複習 |
| 🔗 **延伸學習** | 外部教學影片 11 支（**需要網路**，其他分區都可離線用） |

**側邊工具**：🖊️ 畫筆（12 色）、⏱️ 三個各自獨立的計時器。

### 老師想自己講解的時候

教材教學區有一顆 **🔊 自動朗讀：開／關**。
關掉之後，**切換頁面不會出聲**，但你主動按「再唸一次」還是會播——
只關自動、不關主動。

---

## 七個教學重點

| # | 重點 | 一句話 |
|---|---|---|
| 📏 1 | 認識對稱軸 | 對折會重合的那一條線 |
| 🦋 2 | 生活中的線對稱 | 蝴蝶、愛心和正方形 |
| ✏️ 3 | 看一半畫另一半 | 左邊有什麼，右邊也要一樣 |
| 🔗 4 | 對稱點、對稱邊、對稱角 | 點、邊、角都要相對應 |
| ✳️ 5 | 一個圖形有很多條對稱軸 | 直的、橫的、斜的都算 |
| ▦ 6 | 方格紙完成另一半 | 先找軸、數格子、再連線 |
| 🔎 7 | 生活辨識與任務 | 在教室和校園裡找對稱 |

---

## ⚠️ 延伸學習區的兩件事（2026-08-04 老師確認「11 支全放」）

1. **其中 8 支的上傳者是「Boris的分享小站」**（個人教學頻道）。
   這批影片是從均一教育平台的單元頁找到的，但**均一只是策展、不是自己拍的**，
   學生點下去會進到那個頻道。
2. **3 支是臺北酷課雲的整堂課錄影**，每支 14～15 分鐘，對特教班偏長，
   建議老師陪看或分段播。

每學期開學前跑一次健檢，確認影片沒被下架：

```
python 工具腳本\檢查延伸影片.py
```

---

## 教材規模

78 頁｜7 重點｜21 題｜圖卡 22 組｜主影片 1 支＋短片 4 支｜語音 118 支｜延伸學習 11 支

> 📌 2026-08-03 內容稽核抓到 3 項硬性違規與 4 項內容錯誤，老師裁決全部修完才做網站。
> 修正明細見素材資料夾的 `內容深化版\..._教材使用說明書.md` 與 `..._格式檢查表.md`。

---

## 🛑 給接手維護的人

**改教材＝改素材資料夾的 `內容深化版\`**，網站這邊全部是衍生檔。

```
改 內容深化版\題庫.py
  → 依序重跑：重出簡報 / 重建紙本 / 產出大綱 / 重建教學影片 / 產出data.js
  → 工具腳本\建置素材_圖片壓縮.py、建置語音_全部.py
```

🛑 **不要回頭跑重排頁碼用的組裝腳本**——本單元的 `pages_backup\`／`pages_new\`／
影片原版備份已於 2026-08-04 依老師裁示刪除（254 MB），組裝腳本的「沿用」表寫的是舊頁碼，
來源不在時會組出**每頁都錯卻不報錯**的教材。相關腳本已加 `擋下()` 直接中止。

**改完要跳版本號**，否則瀏覽器會繼續用舊檔：

| 改了什麼 | 要加 1 的地方 |
|---|---|
| 程式或樣式 | `index.html` 底部的 `?v=`（本站 6 處） |
| 重產語音 | `js/config.js` 的 `語音.版本` |
| 換頁圖／圖卡／影片／PDF | `js/config.js` 的 `資源版本` |

🔴 **只改 `config.js` 或 `data.js` 也要跳 `index.html` 的 `?v=`**——
那兩支是用 `js/config.js?v=N` 載入的，不跳 `?v=` 的話回訪者會繼續用快取裡的舊檔，
**等於「加版本號」這個動作本身失效**。部署後在瀏覽器比對
`CONFIG.資源版本` 與 `config.js?v=` 是否對得起來，看本機檔案不算驗過。
